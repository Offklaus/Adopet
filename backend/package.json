{
  "name": "adopet-backend",
  "private": true,
  "version": "0.1.0",
  "description": "API do AdoPet em Node.js puro com PostgreSQL.",
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:create": "node src/db/createDatabase.js",
    "db:migrate": "node src/db/migrate.js",
    "seed": "node src/db/seed.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
